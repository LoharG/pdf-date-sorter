{
  "name": "blackhole-hero-component",
  "private": true,
  "version": "1.0.0",
  "description": "Vanilla-TS port of the BlackHoleHeroSection WebGL effect, built to a static bundle embedded via Streamlit's components.v1.html.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "node build.js"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.6.0"
  }
}
